<h2>Let's Create a New Appearance!</h2>

<%= form_for (@appearance) do |f| %>
    <%= f.label :guest %><br>
    <%= f.collection_select :guest_id, Guest.all, :id, :name %><br>
    <br>
    <%= f.label :episode %><br>
    <%= f.collection_select :episode_id, Episode.all, :id, :date %><br>
    <br>
    <%= f.label :rating %><br>
    <%= f.number_field :rating%><br>
    <br>
    <%= f.submit %>

<% end %>
